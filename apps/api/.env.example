DATABASE_URL="postgresql://ledger:ledger@localhost:5432/orantix_ledger?schema=public"
PORT=3001
UPLOADS_DIR=uploads
JWT_SECRET="change-me-to-a-long-random-string"
# Seed-only: shared password for the dev users seed.ts creates (admin@orantix.local etc). Change before real use.
SEED_PASSWORD=orantix123
